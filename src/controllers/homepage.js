/*import Categories from '../model/homepage.model'
import router from '../router/user.route'

export default class HomeController{
    static async exploreCategories (req, res)  {
        const { category } = req.query
        
        const query = {}
        if(category) {
            query.category = category;
        }

        const 
        
    }   
}*/